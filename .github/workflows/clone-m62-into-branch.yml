name: Clone M62-backport into branch

on:
  schedule:
    - cron: '0 3 * * *'  # Runs daily at 3 AM UTC
  workflow_dispatch:

jobs:
  clone-and-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout m62-backport branch
        uses: actions/checkout@v4
        with:
          ref: m62-backport
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Remove existing files (except .git and workflows)
        run: |
          shopt -s extglob
          rm -rf !(.git|.github)

      - name: Clone upstream repo into branch
        run: |
          git clone https://github.com/ExtremeXT/M62-backport.git

      - name: Commit and push changes
        run: |
          git config "iHagoss@github-actions"
          git config "iHagoss@github.com"
          git add .
          git commit -m "Daily sync: Clone of ExtremeXT/M62-backport"
          git push origin m62-backport
        continue-on-error: true
